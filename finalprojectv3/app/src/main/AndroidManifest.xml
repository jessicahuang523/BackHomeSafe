<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="club.owo.finalproject_v3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bhs_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finalprojectv3">
        <activity
            android:name=".ReportSafeActivity"
            android:exported="false" />
        <activity
            android:name=".ReportUnsafeActivity"
            android:exported="false" />
        <activity android:name=".ScannerActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".ShopRegisterActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.CAMERA" />

</manifest>